# Optional: require this value in header x-api-key for POST /set-message and POST /start-calls
# API_KEY=your-secret-here

# Comma-separated E.164 numbers (no spaces, or spaces trimmed)
CALL_NUMBERS=+15551234567,+15559876543

# Optional (default 3000)
# PORT=3000
